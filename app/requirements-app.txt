# Backend app dependencies, pinned to what the repo .venv has installed.
# The dataset's own requirements (requirements.txt at the repo root) are also needed.
fastapi==0.141.1
uvicorn==0.52.4
httpx==0.28.1
pytest==9.1.1
