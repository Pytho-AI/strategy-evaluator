# Runtime dependencies for the container image. Test-only tools (pytest, playwright)
# and the dataset generator's extras are deliberately absent.
fastapi==0.141.1
uvicorn==0.52.4
pydantic==2.13.5
pandas==3.0.5
numpy==2.5.3
PyYAML==6.0.3
pymupdf==1.28.2
jsonschema==4.26.0
